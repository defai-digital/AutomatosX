/**
 * Execute agent task handler
 *
 * @param rawArgs - Raw command arguments
 * @returns Promise<void>
 *
 * @example
 * ```typescript
 * await runCommand({
 *   agent: 'backend',
 *   task: 'Implement user authentication',
 *   streaming: true,
 *   provider: 'claude'
 * })
 * ```
 */
export declare function runCommand(rawArgs: unknown): Promise<void>;
//# sourceMappingURL=runCommand.d.ts.map