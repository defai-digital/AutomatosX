# Prefer workspace packages over npm registry
# This allows using semantic versions in package.json while still
# resolving to local workspace packages during development
link-workspace-packages=true
prefer-workspace-packages=true

# Save exact versions when adding new packages
save-exact=true

# Hoist dependencies for better compatibility
shamefully-hoist=true
