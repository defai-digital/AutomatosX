# npm configuration for AutomatosX
# Suppress peer dependency warnings for tree-sitter version override
# We intentionally use tree-sitter@0.22.4 across all parsers via overrides
legacy-peer-deps=true
