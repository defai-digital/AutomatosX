{
  "name": "automatosx-v14-migration",
  "version": "14.0.0",
  "private": true,
  "type": "module",
  "description": "v14 platform unification migration workspace",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:migration": "vitest run tests/migration",
    "test:cli": "vitest run packages/cli/tests",
    "typecheck": "tsc --build",
    "clean": "rm -rf .tmp"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "yaml": "^2.8.1",
    "zod": "^3.25.76"
  }
}
